<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="final_V2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:final_V2"
    Shell.FlyoutBehavior="Disabled"
    Title="Reelsbox">

    <!-- Giriş Sayfası (Giriş yaptıktan sonra burası atlanır) -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage"
        Shell.TabBarIsVisible="False"
        Shell.NavBarIsVisible="False" />

    <!-- Ana Uygulama Sekmeleri -->
    <TabBar>
        <Tab Title="Kütüphanem" Icon="dotnet_bot.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>
        <Tab Title="Keşfet" Icon="dotnet_bot.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:DiscoveryPage}"
                Route="DiscoveryPage" />
        </Tab>
        <Tab Title="Profilim" Icon="dotnet_bot.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:ProfilePage}"
                Route="ProfilePage" />
        </Tab>
    </TabBar>

</Shell>
